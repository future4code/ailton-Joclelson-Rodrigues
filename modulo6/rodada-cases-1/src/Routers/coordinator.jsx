export const goToHome = (navigate) => {
  navigate("/");
};

export const goToRequest = (navigate) => {
  navigate("/pedidos");
};

export const goToInventory = (navigate) => {
  navigate("/estoque");
};
